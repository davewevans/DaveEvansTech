@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        string returnUrl = $"{Navigation.ToBaseRelativePath(Navigation.Uri)}";
        Navigation.NavigateTo(string.IsNullOrWhiteSpace(returnUrl) ? "Identity/Account/Register" : $"Identity/Account/Register?returnUrl=/{returnUrl}", true);
    }
}

